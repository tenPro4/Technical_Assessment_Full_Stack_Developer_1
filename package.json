{
  "name": "technical_assessment_full_stack_developer_1",
  "version": "1.0.0",
  "description": "Your task is to create a full-stack application for managing \"items.\" The project consists of a backend API built with Express.js and TypeScript and a frontend application using React.js with Redux Toolkit.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run prisma:generate --workspace=backend\" \"npm:dev --workspace=backend\" \"npm:dev --workspace=frontend\""
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "license": "ISC",
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
